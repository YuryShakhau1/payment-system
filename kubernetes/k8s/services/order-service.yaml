apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-system-order-service
  namespace: payment-system
spec:
  replicas: 1

  selector:
    matchLabels:
      app: payment-system-order-service

  template:
    metadata:
      labels:
        app: payment-system-order-service

    spec:
      containers:
        - name: order-service
          image: ghcr.io/yuryshakhau1/payment-system/order-service:1.0.0

          resources:
            requests:
              memory: "512Mi"
              cpu: "300m"
            limits:
              memory: "512Mi"
              cpu: "300m"

          ports:
            - containerPort: 8083

          env:
            - name: ORDER_SERVICE_PORT
              valueFrom:
                secretKeyRef:
                  name: payment-system-secrets
                  key: ORDER_SERVICE_PORT

            - name: ORDER_SERVICE_DB_HOST
              valueFrom:
                secretKeyRef:
                  name: payment-system-secrets
                  key: ORDER_SERVICE_DB_HOST

            - name: ORDER_SERVICE_DB_PORT
              value: "5432"

            - name: ORDER_SERVICE_DB_NAME
              valueFrom:
                secretKeyRef:
                  name: payment-system-secrets
                  key: ORDER_SERVICE_DB_NAME

            - name: ORDER_SERVICE_DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: payment-system-secrets
                  key: ORDER_SERVICE_DB_USERNAME

            - name: ORDER_SERVICE_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: payment-system-secrets
                  key: ORDER_SERVICE_DB_PASSWORD
            
            - name: CARD_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: payment-system-secrets
                  key: CARD_SECRET_KEY

            - name: KAFKA_HOST_PORT
              valueFrom:
                secretKeyRef:
                  name: payment-system-secrets
                  key: KAFKA_HOST_PORT

            - name: USER_SERVICE_URL
              valueFrom:
                secretKeyRef:
                  name: payment-system-secrets
                  key: USER_SERVICE_URL

            - name: PRODUCT_SERVICE_URL
              valueFrom:
                secretKeyRef:
                  name: payment-system-secrets
                  key: PRODUCT_SERVICE_URL

            - name: SPRING_CONFIG_IMPORT
              value: ""

          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8083
            initialDelaySeconds: 70
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6

          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8083
            initialDelaySeconds: 90
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 5

---
apiVersion: v1
kind: Service
metadata:
  name: payment-system-order-service
  namespace: payment-system
spec:
  type: ClusterIP

  selector:
    app: payment-system-order-service

  ports:
    - port: 8083
      targetPort: 8083